{
\begin{leancode}
  Goal 2 is the infimum of [2, 5). \\
  Proof. \\
  We need to show that (2 is a \_lower bound\_ for [2, 5) \\
  \tab $\land$ ($\forall$ l $\in \mathbb{R}$, l is a \_lower bound\_ for [2, 5) $\rightwhitearrow$ l ≤ 2)). \\
  We show both statements. \\
  \tab - We need to show that (2 is a lower bound for [2, 5)). \\
  \tab \tab We need to show that ($\forall$ c $\in$ [2, 5), 2 $\le$ c). \\
  \tab \tab Take c $\in$ [2, 5). \\
  \tab \tab We conclude that (2 $\le$ c). \\
  \tab - We need to show that \\
  \tab \tab ($\forall$ l $\in \mathbb{R}$, l is a lower bound for [2, 5) $\Rightarrow$ l $\le$ 2). \\
  \tab \tab Take l $\in \mathbb{R}$. Assume that (l is a lower bound for [2, 5)). \\
  \tab \tab We conclude that (l $\le$ 2). \\
  Qed. \\
\end{leancode}
}
