{

\let\oldsum\sum
\renewcommand{\sum}{%
    \mathop{\scalebox{1.5}{$\displaystyle\oldsum$}}\limits
}

\newcommand{\sumDwhat}[1]{
    \sum_{t \in D \cap #1} \sum_{ \tup{x, y} \in t } x
}
\newcommand{\sumDV}{\sumDwhat V}
\newcommand{\sumDH}{\sumDwhat H}
\newcommand{\sumD}{
    \sum_{t \in D} \sum_{ \tup{x, y} \in t } x
}

\begin{tabular}{|p{0.3cm}|p{10cm}|p{8cm}|}
    \hline
    \# & \textbf{Assumption} & \textbf{Explanation} \\
    \hline
    \hline
    1 &
    $
    H = \set{ \set{ \tup{x, y}, \tup{x + 1, y} } \mid x \in 0..8, y \in 0..9 }
    $ &
        Set of possible horizontal tiles \\
    \hline
    2 &
    $
    V = \set{ \set{ \tup{x, y}, \tup{x, y + 1} } \mid x \in 0..9, y \in 0..8 }
    $ &
        Set of possible vertical tiles \\
    \hline
    3 &
    $D \subseteq H \cup V$ is a valid tiling & \\
    \hline
    4 &
    $\abs{D \cap H} = \abs{D \cap V}$ & Assume for contradiction \\
    \hline
    & \textbf{Proposition} & \textbf{Explanation} \\
    \hline
    1 &
    $\abs D = 50$ &
        Our board's size is 100, and every element is a set of size 2, hence: 50 tiles \\
    \hline
    2 &
    $(D \cap H) \cup (D \cap V) = D$ &
        By $D \subseteq H \cup V$ \\
    \hline
    3 &
    $(D \cap H) \cap (D \cap V) = \varnothing$ &
        Because $H$ and $V$ are disjoint \\
    \hline
    4 &
    $|D \cap H| = |D \cap V| = 25$ &
        By the assumption, and by previous two conjectures \\
    \hline
    5 &
    $\sumDV$ is even &
        Every number in the sum appears twice, as the $x$'s of a tile's cells are equal \\
    \hline
    6 &
    $\sumDH$ is odd &
        Here every number appears with its successor by similar reasoning \\
    \hline
    7 &
    $\sumD$ is even &
        By calculation \\
    \hline
    8 &
    $\sumD = \sumDV + \sumDH$ &
        By $(D \cap H) \cup (D \cap V) = D$ and $(D \cap H) \cap (D \cap V) =
        \varnothing$ \\
    \hline
    9 &
    Contradiction & In the previous proposition, the left-hand side is even
    while the right-hand side is odd \\
    \hline
\end{tabular}

}
